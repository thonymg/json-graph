{
  "name": "json-graph",
  "version": "0.0.3",
  "description": "json-graph: Language inspired by the syntax and structure of GraphQL, one can efficiently create and structure JSON objects, providing a more intuitive and streamlined approach to handling and manipulating data in the JSON format.",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "author": "Anthony Michel (@thonymg)",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
